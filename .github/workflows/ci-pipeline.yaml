name: CI Pipeline

on:
  pull_request:
    branches:
      - main
  workflow_dispatch: {}
concurrency:
  group: 'git-autocommit-cli-${{ github.ref }}'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install GitVersion
        id: install_gitversion
        run: |
          dotnet tool install GitVersion.Tool --tool-path ./.gitversion --version 5.12.0

      - name: Determine version
        id: gitversion
        run: ./.gitversion/dotnet-gitversion /output json /showvariable FullSemVer
        shell: bash
        continue-on-error: false
        env:
          PATH: ./.gitversion:$PATH

      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Lint
        run: ./gradlew ktlintCheck

      - name: Build
        env:
          VERSION: ${{ steps.gitversion.outputs.FullSemVer }}
        run: ./gradlew build -Pversion=$VERSION

      - name: Test
        run: ./gradlew test

      - name: Package
        run: ./gradlew shadowJar

      - name: Upload JAR as artifact
        uses: actions/upload-artifact@v3
        with:
          name: autocommit
          path: build/libs/*.jar

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Download build artifact
        uses: actions/download-artifact@v3
        with:
          name: autocommit
          path: ./build/libs

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.gitversion.outputs.FullSemVer }}
          release_name: Release ${{ steps.gitversion.outputs.FullSemVer }}
          draft: false
          prerelease: false

      - name: print dir
        run: |
          ls -la

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./build/libs/autocommit*all.jar
          asset_name: autocommit-${{ steps.gitversion.outputs.FullSemVer }}.jar
          asset_content_type: application/java-archive
